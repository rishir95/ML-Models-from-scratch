-------Coded in Python 3--------

In order to run the program run the tester.py and make sure that the naiveclassifier folder is in the same folder where tester file is.

Use python tester.py -h for the help

e.g. to run:
python tester13.py -t "Z:/Documents/Machine learning/spambasetrain.csv" -s "Z:/Documents/Machine learning/spambaset
est.csv" -n "Z:/abc.txt"