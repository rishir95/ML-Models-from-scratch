-------Coded in Python 3--------

In order to run the program run the knn.py

Use python knn.py -h for the help

e.g. to run:
python knn.py -t "Z:/Documents/Machine learning/reviewstrain.txt" -s "Z:/Documents/Machine learning/reviewstest.txt"

NOTE:
The program takes time to calculate accuracy so wait for a couple of minutes